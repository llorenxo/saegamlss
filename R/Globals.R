#Not Exp

utils::globalVariables(names = c("est", "fdis", "mixed", "RS", '..density..',
                                 '.'),
                       package = "saegamlss",
                       add=FALSE)
